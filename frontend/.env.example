# Optional Deployed Cloud Backend API URL (e.g., Render, Railway, Vercel, Heroku, AWS)
# Example: VITE_API_URL=https://your-backend-api.onrender.com
# If left blank, the desktop app uses the embedded local backend server on port 5050
VITE_API_URL=

# Shipped, NON-SECRET runtime config only. Safe to commit.
# Secrets (MONGO_URI, JWT_SECRET) live in electron/app.secret.env — gitignored,
# provided by the operator at package time, shipped as resources/app.secret.env.
PORT=5050
NODE_ENV=production
